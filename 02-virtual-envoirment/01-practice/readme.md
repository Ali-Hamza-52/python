# Setup Virtual Environment
- pip install virtualenv
- python -m venv env_name_anything // Below is Example
- python -m venv .venv
- pip list > requirement.txt [run whenever you install a new package]

## How we activate this
    we can activate this using ./env_name/Scripts/active this is for windows.
